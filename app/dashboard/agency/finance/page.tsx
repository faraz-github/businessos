// Agency version — re-exports the personal page component.
// The BrandProvider context automatically switches to agency mode
// based on the URL path, so all data fetching uses agency mode.
export { default } from '@/app/dashboard/personal/finance/page';
