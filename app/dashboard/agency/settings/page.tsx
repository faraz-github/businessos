// Agency Settings — re-exports the personal settings page.
// The settings page manages both personal and agency brand profiles
// via an internal mode toggle. Accessible from both sidebar contexts.
export { default } from '@/app/dashboard/personal/settings/page';
